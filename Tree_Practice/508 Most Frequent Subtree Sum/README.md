#### Bugs to Note:

##### 1. list comprehension to show all the equal frequency elements

##### 2. copy.copy() takes too much memory for large files: TimeOut Error

##### 3. count.get(i,0) to set default value or get the saved value

##### 4. for subtree execution: if if statement rather than if else